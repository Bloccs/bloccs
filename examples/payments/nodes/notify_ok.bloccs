[node]
id      = "notify_ok"
version = "0.1.0"
kind    = "sink"

[doc]
intent = "Send a 'charge succeeded' notification email."

[ports.in]
notice = { schema = "ChargeCompleted@1" }

[ports.out]
delivered = { schema = "EmailDelivered@1" }

[effects]
http = { allow = ["api.postmarkapp.com"], methods = ["POST"] }

[contract]
pure_core    = "Payments.Nodes.NotifyOk.transform/2"
effect_shell = "Payments.Nodes.NotifyOk.execute/2"
